version: "3.8"
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: test
      MYSQL_ROOT_PASSWORD: admin1234
    ports:
      - "13307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
